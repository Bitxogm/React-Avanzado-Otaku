import type { NextConfig } from "next";

/**
 * CONFIGURACIÓN PRINCIPAL DE NEXT.JS
 * Aquí puedes modificar el comportamiento interno del framework (Webpack, Turbopack,
 * redirecciones, cabeceras HTTP, optimización de imágenes externas, etc.)
 */
const nextConfig: NextConfig = {
  /* Opciones de configuración (ej. images: { domains: ["github.com"] }) */
};

export default nextConfig;
